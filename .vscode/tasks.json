{
    // See https://go.microsoft.com/fwlink/?LinkId=733558 
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Export from Figma",
            "type": "npm",
            "script": "export --FIGMA_TOKEN=$FIGMA_TOKEN",
            "group": {
                "kind": "build",
                "isDefault": true,
            }
        }
    ]
}